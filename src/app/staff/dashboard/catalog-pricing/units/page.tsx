import Placeholder from '../Placeholder';

export default function UnitsPage() {
  return (
    <Placeholder 
      title="Units of Measurement"
      description="Define standard units of measure (UOM) like PCS, Meters, and Liters for inventory."
    />
  );
}
